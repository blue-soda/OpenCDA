# LGCP Offline Subset Multiseed Summary

This run extends the 11-frame offline subset ablation with extra random-only seeds. Deterministic methods are reused from `20260715_lgcp_carla_comm_aware_baseline_11f`.

- random seeds: `7, 11, 23, 37`
- extra random-only runs: `random_seed11`, `random_seed23`, `random_seed37`
- aggregate random rows: `random_seed_summary.csv`
- deterministic + random mean/std rows: `method_summary.csv`
