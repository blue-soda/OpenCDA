# Flat Raw Byte Accounting

This run preserves the existing flat baseline selected agents and estimates upload bytes from raw PCD point counts. It is used only for common byte-accounting analysis; it does not rerun perception.

- frame rows: `110`
- summary rows: `10`
