# Flat Area-Slice Byte Accounting

This run preserves existing flat baseline selected agents and estimates bytes if only the planned LGCP area cells were transmitted as raw point slices. It does not rerun perception.

- frame rows: `110`
- summary rows: `10`
